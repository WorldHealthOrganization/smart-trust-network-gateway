apiVersion: v1
kind: Service
metadata:
  name: ddcc-gateway
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: ddcc-gateway-postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
